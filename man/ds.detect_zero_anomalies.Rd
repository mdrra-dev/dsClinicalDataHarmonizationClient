% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds_detect_zero_anomalies.r
\name{ds.detect_zero_anomalies}
\alias{ds.detect_zero_anomalies}
\title{Detect suspicious zero-inflation (possible erroneous numeric cast)}
\usage{
ds.detect_zero_anomalies(
  df,
  cols = NULL,
  zero_prop_threshold = 0.3,
  spike_ratio_threshold = 3,
  num_bins = 10,
  nfilter = 5,
  datasources = NULL
)
}
\arguments{
\item{df}{Character string naming the server-side data frame.}

\item{cols}{Character vector restricting the check to specific columns.
\code{NULL} (default): every numeric column.}

\item{zero_prop_threshold, spike_ratio_threshold, num_bins, nfilter}{See
\code{detect_zero_anomaliesDS}.}

\item{datasources}{A list of \code{\link[DSI]{DSConnection-class}} objects.}
}
\value{
Invisible per-server list of per-column zero-anomaly stats.
}
\description{
Flags numeric columns where zeros look like a cast artefact
  rather than genuine clinical zeros -- see \code{detect_zero_anomaliesDS}
  for the two heuristics used. Run this after any type coercion step
  (\code{ds.force_convert_types}, \code{ds.cast_NA}, imputation) and,
  generally, as part of routine data-quality review.

Server function called: \code{detect_zero_anomaliesDS}
}
