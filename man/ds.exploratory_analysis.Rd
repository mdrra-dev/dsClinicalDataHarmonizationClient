% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds_exploratory_analysis.r
\name{ds.exploratory_analysis}
\alias{ds.exploratory_analysis}
\title{Non-disclosive exploratory analysis of a server-side data frame}
\usage{
ds.exploratory_analysis(
  df,
  numeric_cols = NULL,
  categorical_cols = NULL,
  group_col = NULL,
  num_bins = 20,
  nfilter = 5,
  datasources = NULL
)
}
\arguments{
\item{df}{Character string naming the server-side data frame.}

\item{numeric_cols, categorical_cols}{Character vectors restricting which
columns to summarize. \code{NULL} (default): every numeric / every
non-numeric column respectively.}

\item{group_col}{Optional column for group-wise numeric means (e.g. a
visit number, for a longitudinal trend).}

\item{num_bins}{Number of histogram bins per numeric column. Default 20.}

\item{nfilter}{Disclosure/stability floor.}

\item{datasources}{A list of \code{\link[DSI]{DSConnection-class}} objects.}
}
\value{
Invisible per-server list, the raw \code{exploratory_analysisDS} output.
}
\description{
Per-site and aggregated descriptive statistics, distributions,
  category frequencies, correlations, and group-wise trends, using the
  server function \code{exploratory_analysisDS}. Only aggregate statistics
  ever return -- see \code{exploratory_analysisDS} for the exact
  disclosure-control convention (nfilter-suppressed cells/bins).

  Usually called via \code{ds.harmonization_explore()} rather than
  directly -- that wrapper defaults to the cleaned dataset and builds a
  full figure set. Use this function directly for finer control over
  which columns/grouping to summarize.

Server function called: \code{exploratory_analysisDS}
}
